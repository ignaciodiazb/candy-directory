<% content_for :title, "Admin — Nueva categoría" %>

<div class="admin-header">
  <h1>Nueva categoría</h1>
  <%= link_to "← Volver", admin_categories_path, class: "btn-secondary" %>
</div>

<%= render "form", category: @category %>
